services:
  app:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "8000:8000"
    volumes:
      - /Users/ermiaskefelegn/Development/Python Projects/Django_Recipe_App/app:/app:rw
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000" 